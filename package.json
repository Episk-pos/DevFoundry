{
  "private": true,
  "scripts": {
    "lint:ascii": "tsx scripts/format-ascii.ts --check",
    "format:ascii": "tsx scripts/format-ascii.ts --write",
    "test": "vitest run"
  },
  "devDependencies": {
    "glob": "^11",
    "tsx": "^4",
    "typescript": "~5.6.2",
    "vitest": "^3"
  }
}
