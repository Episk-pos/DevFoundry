apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
data:
  DATABASE_URL: "postgresql://postgres:secret@postgres:5432/chat"
  NODE_ENV: "production"
  CORS_ORIGIN: "http://localhost:8080"
  LOG_LEVEL: "info"
